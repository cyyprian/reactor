import createElements from "./helpers/createElements";
import * as names from "../elements/index";
import * as payloads from "../elements/payloads";

const elements = createElements(names, payloads);

export default elements;
